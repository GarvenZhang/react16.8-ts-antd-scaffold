import * as React from 'react';
import {} from 'react';

import './index.scss';

interface Props {}
const ParameterConfiguration: React.FC<Props> = ({}) => {
  return <div className="">参数配置</div>;
};

export default ParameterConfiguration;
